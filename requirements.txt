# Core dependencies
trimesh
numpy
scipy

# UniRig dependencies
transformers==4.51.3
python-box
einops
omegaconf
pytorch_lightning
lightning
addict
timm
huggingface_hub
wandb
fast-simplification

# Mesh processing
open3d
pyrender

# Note: bpy is no longer needed - we use bundled Blender instead
# Note: torch-cluster, torch-scatter, and spconv are installed via lib/unirig/install.py
# Note: flash_attn is optional and installed via lib/unirig/install.py if available
