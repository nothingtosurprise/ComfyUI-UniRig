# ComfyUI-UniRig - Environment Configuration
# Run: comfy-env install

[local.cuda]
torch-scatter = "2.1.2"
torch-cluster = "1.6.3"
# spconv only has wheels up to cu126 - use explicit package name
spconv-cu126 = "*"
# flash-attn for UniRig skinning model
flash-attn = "2.8.3"
