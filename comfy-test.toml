[test]
levels = ["syntax", "install", "registration", "validation", "instantiation", "static_capture", "execution"]

[test.workflows]
timeout = 300
cpu = []
gpu = "all"

[test.platforms]
linux = true
macos = false
windows = true
windows_portable = true
