[test]
levels = ["syntax", "install", "registration", "instantiation", "static_capture", "execution"]

[test.workflows]
timeout = 300
cpu = []        # No CPU-only workflows - skip CPU jobs
gpu = "all"     # All workflows need GPU

[test.platforms]
linux = true
macos = false #requires cuda
windows = true
windows_portable = true